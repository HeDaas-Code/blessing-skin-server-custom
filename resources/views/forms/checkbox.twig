<label for="{{ id }}">
  <input
    type="checkbox"
    {{ value ? 'checked="true"' : '' }}
    id="{{ id }}"
    name="{{ id }}"
    class="terra-check"
    {{ disabled ? 'disabled=disabled' : '' }}
    value="true"
  > {{ label }}
</label>
