# 项目持久记忆 Project Memory

> 本文件由 dsh-memoir 插件维护：记录本项目历次会话的工作归纳、经验教训与行动指南，
> 作为未来 AGENTS 接手本项目时的行动指南；它是人类可读的投影，不是 system prompt 的完整注入内容。
> 新会话只注入有界的 Hot Memory，完整历史通过 memoir_read 按需检索。

## 工作记录 Work Log

- [2026-09-17 23:48] [工作记录] Blessing Skin Server 本地开发环境就绪 — 克隆 dev 分支（HEAD 52f6fefe）到 /home/hedass/文档/项目/blessing-skin-server；安装 PHP 8.3.6 + 所需扩展与 Composer 2.7.1，composer install 完成（Laravel 10.48.25，169 条路由）；npm 全局安装 yarn 1.22.22，yarn install --frozen-lockfile 与 yarn build 均成功。.env 指向 SQLite（database/database.sqlite），走完安装向导（站点名 Blessing Skin Dev，超管 admin@example.com），php artisan serve 监听 127.0.0.1:8000 返回 200。生产部署形态（Docker/MySQL）尚未确定。
- [2026-09-17 23:48] [工作记录] normify 结构数据项目已初始化 — 结构数据目录：/home/hedass/文档/项目/blessing-skin-server/normify-blessing-skin-server（slug blessing-skin-server，slug 由 normify_project_init 生成）。默认 policy.yml 两条规则：core-acyclic(error)、core-no-deprecated-target(warning)。误建在 /home/agentuser/workspace 下的同名空目录已删除。根模块与模块树在后续轮次批量写入。
- [2026-09-17 23:53] [工作记录] normify 模块树骨架已落地（根 + 15 个一级域） — 在 normify-blessing-skin-server 写入 16 个 active 模块：根 bss（repository 指向 GitHub）+ 一级域 bss.http/exception/user/player/texture/admin/plugin/option/i18n/notify/security/event/frontend/view/infra；根层已写 renders/bss.json（mode=groups，四组：请求入口/业务域/平台能力/表现与交付）。每个模块均带 source 证据与 normify_fingerprint 计算的指纹，revision=52f6fefe…。
- [2026-09-18 00:03] [工作记录] normify：http/exception/user 子树已建成 — 已写入并通过 L1：bss.http 子树 17 个模块（kernel、routing 容器+4 叶子、middleware 容器+4 分组叶子、composers 容器+3 叶子）、bss.exception 2 个、bss.user 13 个（auth 容器+5 流程叶子、profile 容器+2、dashboard、score、oauth、model）。所有容器均写了 renders/*.json（order/groups/layers/grid + 中英导语）。
- [2026-09-18 00:17] [工作记录] normify 架构图已生成（149 模块 / 288 API） — 模块树建成并渲染：normify-blessing-skin-server 下 149 个模块、288 条 API、59 条依赖、34 个容器 layout；normify_validate 0 error（18 warning，其中 leaf-too-coarse 15 条、dep/unanchored 1 条、root-no-source 1 条）。产物：normify.html（/home/hedass/文档/项目/blessing-skin-server/normify-blessing-skin-server/normify.html）、tree.json、outline.md、api-index.json、receipt.json。当前 max_depth=4。
- [2026-09-18 00:31] [工作记录] BS 自定义认证服务器的实现机制 — 核心仓库不实现 Yggdrasil 协议（repo 内 grep yggdrasil 仅前端测试 mock 出现插件名 yggdrasil-api）。自定义认证服务器由外部插件 yggdrasil-api 实现，核心提供四块底座：1) User + HasPassword + Cipher 密码算法族做账号校验；2) Hook::addRoute 监听 ConfigureRoutes 事件（RouteServiceProvider map 完 web/api/static 后广播）让插件注入协议路由；3) TextureController@json 的 {player}.json 材质清单 + textures/{hash}/avatar/preview 供皮肤下发；4) routes/api.php 的 Passport OAuth 接口 + 16 个 scope 供第三方应用。定制化候选方向：自研内置 Yggdrasil 端点或自研认证插件。
- [2026-09-18 00:39] [工作记录] yggdrasil-api 插件已纳入架构图（双树） — 克隆官方插件 bs-community/yggdrasil-api（HEAD 7eb2ceb1）到 plugins/yggdrasil-api 下，并在同一 normify 项目中新增第二棵树 yggdrasil-api（14 模块：bootstrap/utils/auth/session/profile/token/middleware×3/exception/config/i18n），含 10 条跨树箭头指向 bss.*（user/player/texture/option/event/plugin）。全项目现在 tree_count=2、163 模块、311 API、74 deps，validate 0 error，build+render 完成（normify.html 472KB）。
- [2026-09-18 01:24] [工作记录] 双向容灾官方认证（绑定式官方档案）已实现 — 在 yggdrasil-api 插件实现微软账号绑定与 P0/P1/P2 容灾路由：新增 ygg_bindings 表（令牌 Crypt 加密）、Binding 模型、MicrosoftAuth（MSA→XBL→XSTS→Minecraft 链）、OfficialRouter（健康检查/熔断）、BindingController（用户中心+后台页面）；AuthController.authenticate 接入路径决策，Profile::getUuidFromName 支持同名角色取官方 UUID；callbacks 建表+默认选项，config 页新增官方路由表单。插件 package.json require 改为 ^5.0.0 || ^6.0.0 以兼容 BS6。已通过 php -l、plugin:enable（建表成功）、route:list（新路由注册）、normify change 关闭 verified（0 error）。
- [2026-09-18 01:30] [工作记录] 官方皮肤同步与用户中心菜单入口完成 — yggdrasil-api 增加 SkinSynchronizer：绑定时/手动触发时下载微软档案 ACTIVE 皮肤与披风，写入纹理库（私有、不扣积分、按 hash 去重）并应用到同名角色 tid_skin/tid_cape。绑定页新增"同步官方皮肤"按钮（POST user/yggdrasil-binding/sync），用户中心侧边栏新增"微软账号绑定"菜单（Hook::addMenuItem category=user）。normify 更新为 171 模块/312 API/88 deps（13 跨树），validate 0 error，HTML 已重渲染。
- [2026-09-18 02:07] [工作记录] 本地测试账号与角色已建 — 创建测试账号 test@example.com / Test@123456（昵称 TestUser，普通用户 verified，积分 1000），并创建角色 test_player。已通过 POST /api/yggdrasil/authserver/authenticate 实调验证：P0 路径返回 offline UUID 与 availableProfiles 正常，AuthController 决策链路未破坏原逻辑。
- [2026-09-18 09:25] [工作记录] 本地已预填 Azure 官方认证配置 — 本地插件选项已写入：ygg_official_client_id=1a5583b8-63f1-4d81-924f-1459c2f06040、ygg_official_client_secret=已填入（值不入库）、ygg_official_redirect_uri=http://localhost:8000/user/yggdrasil-binding/callback、ygg_official_enabled=true。authorize URL 自检通过。待办：浏览器用 localhost:8000 登录 test 账号完成微软绑定联调；联调后到 Azure 轮换该 secret（聊天中暴露过）。
- [2026-09-18 09:48] [工作记录] 官方绑定角色名冲突解决页已实现 — yggdrasil-api 新增冲突解决流程：绑定成功后若官方角色名与站内角色名不一致，跳转 /user/yggdrasil-binding/resolve 让玩家选择（保留原名称 或 将某一站内角色改名为官方名称并应用官方档案+同步皮肤）；若官方名已被其他用户占用则只允许保留。ygg_bindings 表新增 skin_url/skin_variant/cape_url 列（bind 时保存，resolve 改名后无需再调微软接口即可同步皮肤）。改名走 player.renaming/player.renamed 事件并校验 PlayerName 规则与唯一性。binding.ui 已补充 8 条 HTTP API；normify validate 0 error 已重渲染。

## 经验教训 Lessons Learned

- [2026-09-17 23:48] [经验教训] 本机环境与 normify 使用要点 — sudo 实为 doas 别名，不支持 `sudo ENV=x cmd`，须写 `sudo env DEBIAN_FRONTEND=noninteractive apt-get install ...`。normify 默认 rootDir 为 /home/agentuser/workspace（其中 项目/projects 是指向 /home/hedass/文档/项目 的软链），可用 dir 参数把结构数据目录放进仓库内。normify_fingerprint 对空 source 返回 e3b0c442…b855（完整 SHA-256 hex）。仓库体量：PHP 后端约 7.8k 行、前端 TS/TSX 约 9.3k 行、Twig 模板约 100 个文件。
- [2026-09-17 23:53] [经验教训] normify_module_batch 写入要点 — upsert 模式的 items 形如 [{ frontmatter }]；根模块 parent 传 JSON null、非根由 id 去尾段推导。指纹必须逐模块调用 normify_fingerprint（repoRoot + 该模块 source 列表）获得，不能手算；一次 run_code 内可连续多次调用。source 路径缺失会让工具返回 missing 并导致校验失败。
- [2026-09-18 00:03] [经验教训] normify 批量写入的三个硬约束 — 1) normify_module_batch 是整批原子：任一模块 L1 失败则全批不落盘，故需小批或分组写入才能定位问题模块。2) dep/target-missing：deps.to 指向的模块必须已存在或在同一批内，因此父容器、被依赖叶子要先写。3) structure/label-too-long：dep label.en 必须 ≤30 字符（“Listen to registration completed” 32 字符即被拒）。4) normify_layout_upsert 对还没有子模块的模块报 layout/not-container，渲染数据必须在子模块写入之后写。
- [2026-09-18 02:07] [经验教训] BS User 模型创建需先填 password 占位 — 用 Eloquent 直接创建 User 时 users.password 是 NOT NULL 且不在 $fillable，但直接属性赋值可写。正确顺序：$user->password="temporary"; $user->save(); 再 $user->changePassword($raw)（内部走 cipher->hash + Filter）。若先 save 再 changePassword 会触发 Integrity constraint violation: users.password。
- [2026-09-18 09:17] [经验教训] Azure 重定向 URI 必须用 localhost 且三处一致 — 微软 OAuth 绑定（yggdrasil-api）本地联调约束：Azure 只给 http://localhost 开 HTTP 回环特例，http://127.0.0.1 会被拒。因此本地填 http://localhost:8000/user/yggdrasil-binding/callback，且三处必须完全一致：Azure 应用注册 Web 重定向 URI、插件配置 ygg_official_redirect_uri、浏览器访问用 localhost。当前站点 site_url 为 http://127.0.0.1:8000，插件自动生成的重定向会是 127.0.0.1，必须手动覆盖或改 site_url。Azure 应用还需选个人 Microsoft 账户并加 XboxLive.signin 权限。
- [2026-09-18 09:37] [经验教训] login_with_xbox 403 Invalid app registration = Azure Client ID 需 Mojang 白名单 — yggdrasil-api 官方绑定 403 最终根因（已由 Minecraft 官方文档确认）：Mojang 现已要求新创建的 Azure 应用先申请白名单才能调用 api.minecraftservices.com，未批准的应用在 login_with_xbox 返回 {"errorMessage":"Invalid app registration"}。与 XboxLive.signin 权限无关（不用再找它）。修复：填 https://aka.ms/mce-reviewappid 表单（邮箱、应用名、Client ID、Tenant ID、关联网站/域名、用途说明），等待 Mojang 审核约一周。官方说明文章 https://help.minecraft.net/hc/en-us/articles/16254801392141；参考实现文档 mcproto authentication.md、minecraft-launcher-lib microsoft_login.rst。审核期间站内 P0 离线认证不受影响。

## 行动指南 Action Guide

- [2026-09-17 23:48] [行动指南] 下一步：normify 建树与渲染 — 架构图已覆盖 bss + yggdrasil-api 双树，官方认证容灾变更已 verified。部署注意事项：需在 Azure AD 注册 Web 应用并配置 ygg_official_client_id/secret/redirect_uri，官方路由默认关闭（ygg_official_enabled=false，此时绑定用户走 P2 本地兜底官方 UUID）。后续可选：绑定时同步官方皮肤、为绑定页加用户中心菜单入口、锚定 47 条未锚定箭头。
- [2026-09-18 09:35] [行动指南] 绑定失败排查：等用户重试后读 laravel 日志 — 微软绑定回调失败排查中：已开启 .env YGG_VERBOSE_LOG=true，BindingController 两处 catch 已加 Log::error 写 laravel 日志（含 trace）。已排除项：Binding 模型/Crypt/DB 读写正常、SkinSynchronizer 无皮肤路径正常、本机到 login.microsoftonline.com/user.auth.xboxlive.com/xsts/api.minecraftservices.com/textures.minecraft.net 网络可达（HTTP 200/404/403/400）。下一步：用户重试绑定后读 storage/logs/laravel-*.log 拿真实异常。注意：ygg 日志通道默认是 NullHandler（YGG_VERBOSE_LOG=false 时静默丢弃），排查必须显式开。
