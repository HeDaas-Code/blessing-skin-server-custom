# 项目持久记忆 Project Memory

> 本文件由 dsh-memoir 插件维护：记录本项目历次会话的工作归纳、经验教训与行动指南，
> 作为未来 AGENTS 接手本项目时的行动指南；它是人类可读的投影，不是 system prompt 的完整注入内容。
> 新会话只注入有界的 Hot Memory，完整历史通过 memoir_read 按需检索。

## 工作记录 Work Log

- [2026-09-17 23:48] [工作记录] Blessing Skin Server 本地开发环境就绪 — 克隆 dev 分支（HEAD 52f6fefe）到 /home/hedass/文档/项目/blessing-skin-server；安装 PHP 8.3.6 + 所需扩展与 Composer 2.7.1，composer install 完成（Laravel 10.48.25，169 条路由）；npm 全局安装 yarn 1.22.22，yarn install --frozen-lockfile 与 yarn build 均成功。.env 指向 SQLite（database/database.sqlite），走完安装向导（站点名 Blessing Skin Dev，超管 admin@example.com），php artisan serve 监听 127.0.0.1:8000 返回 200。生产部署形态（Docker/MySQL）尚未确定。
- [2026-09-17 23:48] [工作记录] normify 结构数据项目已初始化 — 结构数据目录：/home/hedass/文档/项目/blessing-skin-server/normify-blessing-skin-server（slug blessing-skin-server，slug 由 normify_project_init 生成）。默认 policy.yml 两条规则：core-acyclic(error)、core-no-deprecated-target(warning)。误建在 /home/agentuser/workspace 下的同名空目录已删除。根模块与模块树在后续轮次批量写入。
- [2026-09-17 23:53] [工作记录] normify 模块树骨架已落地（根 + 15 个一级域） — 在 normify-blessing-skin-server 写入 16 个 active 模块：根 bss（repository 指向 GitHub）+ 一级域 bss.http/exception/user/player/texture/admin/plugin/option/i18n/notify/security/event/frontend/view/infra；根层已写 renders/bss.json（mode=groups，四组：请求入口/业务域/平台能力/表现与交付）。每个模块均带 source 证据与 normify_fingerprint 计算的指纹，revision=52f6fefe…。
- [2026-09-18 00:03] [工作记录] normify：http/exception/user 子树已建成 — 已写入并通过 L1：bss.http 子树 17 个模块（kernel、routing 容器+4 叶子、middleware 容器+4 分组叶子、composers 容器+3 叶子）、bss.exception 2 个、bss.user 13 个（auth 容器+5 流程叶子、profile 容器+2、dashboard、score、oauth、model）。所有容器均写了 renders/*.json（order/groups/layers/grid + 中英导语）。
- [2026-09-18 00:17] [工作记录] normify 架构图已生成（149 模块 / 288 API） — 模块树建成并渲染：normify-blessing-skin-server 下 149 个模块、288 条 API、59 条依赖、34 个容器 layout；normify_validate 0 error（18 warning，其中 leaf-too-coarse 15 条、dep/unanchored 1 条、root-no-source 1 条）。产物：normify.html（/home/hedass/文档/项目/blessing-skin-server/normify-blessing-skin-server/normify.html）、tree.json、outline.md、api-index.json、receipt.json。当前 max_depth=4。
- [2026-09-18 00:31] [工作记录] BS 自定义认证服务器的实现机制 — 核心仓库不实现 Yggdrasil 协议（repo 内 grep yggdrasil 仅前端测试 mock 出现插件名 yggdrasil-api）。自定义认证服务器由外部插件 yggdrasil-api 实现，核心提供四块底座：1) User + HasPassword + Cipher 密码算法族做账号校验；2) Hook::addRoute 监听 ConfigureRoutes 事件（RouteServiceProvider map 完 web/api/static 后广播）让插件注入协议路由；3) TextureController@json 的 {player}.json 材质清单 + textures/{hash}/avatar/preview 供皮肤下发；4) routes/api.php 的 Passport OAuth 接口 + 16 个 scope 供第三方应用。定制化候选方向：自研内置 Yggdrasil 端点或自研认证插件。
- [2026-09-18 00:39] [工作记录] yggdrasil-api 插件已纳入架构图（双树） — 克隆官方插件 bs-community/yggdrasil-api（HEAD 7eb2ceb1）到 plugins/yggdrasil-api 下，并在同一 normify 项目中新增第二棵树 yggdrasil-api（14 模块：bootstrap/utils/auth/session/profile/token/middleware×3/exception/config/i18n），含 10 条跨树箭头指向 bss.*（user/player/texture/option/event/plugin）。全项目现在 tree_count=2、163 模块、311 API、74 deps，validate 0 error，build+render 完成（normify.html 472KB）。
- [2026-09-18 01:24] [工作记录] 双向容灾官方认证（绑定式官方档案）已实现 — 在 yggdrasil-api 插件实现微软账号绑定与 P0/P1/P2 容灾路由：新增 ygg_bindings 表（令牌 Crypt 加密）、Binding 模型、MicrosoftAuth（MSA→XBL→XSTS→Minecraft 链）、OfficialRouter（健康检查/熔断）、BindingController（用户中心+后台页面）；AuthController.authenticate 接入路径决策，Profile::getUuidFromName 支持同名角色取官方 UUID；callbacks 建表+默认选项，config 页新增官方路由表单。插件 package.json require 改为 ^5.0.0 || ^6.0.0 以兼容 BS6。已通过 php -l、plugin:enable（建表成功）、route:list（新路由注册）、normify change 关闭 verified（0 error）。
- [2026-09-18 01:30] [工作记录] 官方皮肤同步与用户中心菜单入口完成 — yggdrasil-api 增加 SkinSynchronizer：绑定时/手动触发时下载微软档案 ACTIVE 皮肤与披风，写入纹理库（私有、不扣积分、按 hash 去重）并应用到同名角色 tid_skin/tid_cape。绑定页新增"同步官方皮肤"按钮（POST user/yggdrasil-binding/sync），用户中心侧边栏新增"微软账号绑定"菜单（Hook::addMenuItem category=user）。normify 更新为 171 模块/312 API/88 deps（13 跨树），validate 0 error，HTML 已重渲染。
- [2026-09-18 02:07] [工作记录] 本地测试账号与角色已建 — 创建测试账号 test@example.com / Test@123456（昵称 TestUser，普通用户 verified，积分 1000），并创建角色 test_player。已通过 POST /api/yggdrasil/authserver/authenticate 实调验证：P0 路径返回 offline UUID 与 availableProfiles 正常，AuthController 决策链路未破坏原逻辑。
- [2026-09-18 09:25] [工作记录] 本地已预填 Azure 官方认证配置 — 本地插件选项已写入：ygg_official_client_id=1a5583b8-63f1-4d81-924f-1459c2f06040、ygg_official_client_secret=已填入（值不入库）、ygg_official_redirect_uri=http://localhost:8000/user/yggdrasil-binding/callback、ygg_official_enabled=true。authorize URL 自检通过。待办：浏览器用 localhost:8000 登录 test 账号完成微软绑定联调；联调后到 Azure 轮换该 secret（聊天中暴露过）。
- [2026-09-18 09:48] [工作记录] 官方绑定角色名冲突解决页已实现 — yggdrasil-api 新增冲突解决流程：绑定成功后若官方角色名与站内角色名不一致，跳转 /user/yggdrasil-binding/resolve 让玩家选择（保留原名称 或 将某一站内角色改名为官方名称并应用官方档案+同步皮肤）；若官方名已被其他用户占用则只允许保留。ygg_bindings 表新增 skin_url/skin_variant/cape_url 列（bind 时保存，resolve 改名后无需再调微软接口即可同步皮肤）。改名走 player.renaming/player.renamed 事件并校验 PlayerName 规则与唯一性。binding.ui 已补充 8 条 HTTP API；normify validate 0 error 已重渲染。
- [2026-09-18 10:56] [工作记录] 项目已归档到新 GitHub 仓库 — 创建私有仓库 https://github.com/HeDaas-Code/blessing-skin-server-custom（本地 /home/hedass/文档/项目/blessing-skin-server-custom），初始提交含主程序、plugins/yggdrasil-api 定制插件、normify-blessing-skin-server 架构图与自定义 README。安全措施：排除 .env、database/*.sqlite、storage 运行时内容、OAuth 密钥、日志；已 git grep 验证无 Azure 客户端密码/APP_KEY/gh token 泄漏；.env.testing 为上游测试配置（dummy 密钥）保留。插件作为普通目录提交（已移除内嵌 .git）。待办：如需公开仓库，GitHub Settings→Danger Zone→Change visibility。
- [2026-09-18 12:13] [工作记录] yggdrasil-api 实现设备码+授权码双路微软绑定 — 已在 yggdrasil-api 完成双路认证：MicrosoftAuth 新增 startDeviceCode/pollDeviceCode/deviceRefreshTokens，verify 按 ygg_official_auth_method 分流；BindingController 新增 showDevice/startDevice/pollDevice，device.twig 显示设备码并自动轮询；后台新增 ygg_official_auth_method（device_code|authorization_code，默认 device_code）与 ygg_official_device_client_id（默认 Prism 公开 ID c36a9fb6-4f2a-41ff-90bd-ae7cc92031eb）。验证：全插件 php -l、YAML、Twig 解析、route:list 均通过，DB 默认项已写入。注意：设备码绑定产生的 refresh_token 与 Prism client_id 绑定，后台切回授权码后旧绑定同步会失败，生产需用自己白名单应用并重新绑定。
- [2026-09-18 13:23] [工作记录] 官方同步无角色时自动创建官方角色 — SkinSynchronizer::sync 已实现：按 uid+官方名找不到角色时自动创建 Player（uid/name/tid_skin/tid_cape=0，触发 PlayerWasAdded），再应用官方皮肤/披风；官方名被他人占用时返回 name-occupied 并阻止创建。BindingController 三个同步调用点（授权码回调、设备码轮询、手动 sync）均检查该返回值并向用户显示错误。已验证：无角色场景自动创建成功（测试后清理），占用场景返回 name-occupied。
- [2026-09-18 13:36] [工作记录] GitHub 仓库补传 yggdrasil-api 插件代码 — 已把 plugins/yggdrasil-api 全部 52 个文件提交并推送到 https://github.com/HeDaas-Code/blessing-skin-server-custom（commit 83a3588，分支 master）。纠偏：该仓库初始提交并未包含插件代码——plugins/.gitignore 的 `*` 把插件整体忽略了，`!yggdrasil-api/` 未能使其重新纳入跟踪，必须用 git add -f 强制添加。后续如需再传插件文件，直接 git add -f plugins/yggdrasil-api。
- [2026-09-18 14:04] [工作记录] 莱茵生命前端重设计 AgentTeams 计划已建为 staged — 已将莱茵生命前端重设计建为 staged AgentTeams 团队 rhine-lab-ui-redesign（state_dir: .agent-teams/rhine-lab-ui-redesign）：6 名成员（theme-engineer/public-surfaces/data-surfaces/react-themer/motion-a11y/qa-verifier）、8 个任务 DAG（t1 侦察基线→t2 令牌主题→t3 全局外壳→t4 公共页面/t5 数据面表单/t6 React 组件→t7 动效可访问性→t8 构建 QA）。契约：faction=rhine-lab、depth=moderate、tone lane=cool-light-laboratory、功能不变。当前 phase=staged，等 Web 审批后才执行，尚未改动任何产品代码。
- [2026-09-18 14:27] [工作记录] rhine-lab-ui-redesign 已启动，S0 完成、S1 进行中 — rhine-lab-ui-redesign 团队已批准并 running。t1（S0 侦察与基线）已完成并记录：lint 通过、test 通过（39 套件/366 用例）、build 通过（2 个既有子编译警告）、type:check 基线失败（17 个既有 React 类型漂移错误，非本次引入，t8 QA 应采用基线相对验收“无新增类型错误”）；侦察产物与 terra-faction-ui grammar/scripts/tokens 已持久化到 docs/terra-faction-ui/（37 文件，未改任何产品代码）。t2（S1 令牌与全局主题）已由调度器派发给 theme-engineer，in_progress。
- [2026-09-18 14:52] [工作记录] S1 令牌与全局主题已完成（terra.css + webpack 入口 + 根属性） — t2 完成：新建 resources/assets/src/styles/terra.css（约 450 行，cool-light-laboratory 令牌、.dark-mode 覆盖、.terra-pane/band/gate、表单/按钮/表格/下拉/分页/卡片/侧栏/顶栏莱茵几何、2px focus-visible、tabular-nums、reduced-motion）；webpack.config.ts 的 style 入口在 AdminLTE light 与 fontawesome 之后追加 terra.css；7 个 base.twig（home/admin/user/auth/skinlib/setup/errors）的 <html> 加 data-terra-faction=rhine-lab data-terra-depth=moderate。验证：build/lint/test 全 PASS，type:check 仍为基线既有 17 错误（0 新增）。改动仅 7 个 base.twig + webpack.config.ts + 新增 terra.css，无功能/JS 钩子变化。
- [2026-09-18 15:21] [工作记录] t3 全局外壳因上游限流失败，待重试 — rhine-lab-ui-redesign 的 t3（S2 全局外壳）被标记 failed：theme-engineer 子代理执行时命中上游限流/容量保护（PI_AI_ERROR），任务 attempt 1 失败；theme-engineer 成员状态回到 idle/ready。t3 尚未重试，下游 t4/t5/t6 仍被阻塞。此前 t1/t2 已完成并记录，t4 已收到 spectre 入口提示。
- [2026-09-18 16:34] [工作记录] S3 公共页面已完成（含 spectre 入口接入 terra.css） — t4 完成：webpack.config.ts 的 spectre 入口追加 terra.css（setup/errors 页现已加载 terra 令牌，产物 spectre.ec86aea.css 已确认含 --terra-field）；home.css 重写首页（splash 磨砂诊断窗格、main-button 审阅闸门、intro/footer-wrap 实验台/安全纸带，保留背景图与 fixed-bg）；terra.css 第 7 节覆盖认证卡片、OAuth scopes、错误/安装 .terra-panel、Spectre 控件。四个 twig（auth/errors/setup base + passport authorize）仅加性 class 钩子，未改表单字段/错误语义/data-* 钩子。验证：build/lint/test PASS，type:check 仍为基线 17 错误（0 新增）。
- [2026-09-18 16:57] [工作记录] S4 数据面与表单已完成（CSS 第 9 段 + 3 个 Twig class 钩子） — t5 完成（data-surfaces 报告后队长接管记录）：terra.css 新增第 9 段 Data surfaces & forms（卡片霜化诊断窗格、彩色 AdminLTE card-header 中和为矿物白面、card-danger 保留红色 hazard band、按钮主/次/危险几何、表格 tabular-nums+矿物规线、select 自绘刻度 chevron、.terra-check 校准刻度、.terra-latch containment latch、.terra-swatch 样本舱色块）；forms/checkbox.twig、admin/widgets/dashboard/notification.twig、admin/customize.twig 仅加 class，未改 name/value/行为。验证：build/lint PASS、type:check 基线 17 错误 0 新增；yarn test 有 1 失败 tests/views/user/Dashboard.test.tsx，根因是 t6 react-themer 并发改 InfoBox.tsx（data-terra-specimen），交 t8 复核。
- [2026-09-18 17:05] [工作记录] S5 React 组件主题已完成（terra.ts helper + 签名控件） — t6 完成：terra.css 追加第 8 节 React 组件段与 8b 签名控件（.terra-chamber-toggle/menu 样本舱选择器、.terra-stepper 校准步进器、.terra-sample-chamber 样本舱上传区、.terra-gate 审阅闸门、modal/toast/info-box 霜化诊断窗格）；新建 resources/assets/src/styles/terra.ts 共享 token helper（--terra-* 常量 + pane/microLabel/tabular 片段）；所有 emotion/inline 硬编码色替换为 var(--terra-*)；FilterSelector→Sample chamber selector、Pagination→Calibration stepper、Upload/SignButton→Review gate；未改状态/props/事件/API。验证：lint/test/build 全 PASS，type:check 基线 17 错误 0 新增。之前 t5 观察到的 Dashboard.test.tsx 偶发失败在 t6 完成后已消除。
- [2026-09-18 17:51] [工作记录] S6 动效与可访问性已完成（t7） — t7 完成（motion-a11y 报告后队长接管记录）：terra.css 新增入场动效（terra-aperture 550ms + terra-calibrate 650ms 单次，cubic-bezier 无弹跳）、选中样本轨 2s 单次 settle、reduced-motion 下 animation:none 立即最终态、:focus-visible 2px chartreuse 外偏轮廓（!important 覆盖 Bootstrap/AdminLTE）、交互目标 ≥40×40px；.dark-mode 新增恒暗 ink-solid/on-solid 令牌修复白字对比、规则线提亮至 3:1+；移动端 ≤767.98px 顶栏 sticky + 侧栏固定抽屉。home.css 仅 .main-button 改 solid 令牌。验证：build/lint/test PASS，type:check 基线 17 错误 0 新增。t8（S7 QA）依赖已满足待派发。
- [2026-09-18 18:52] [工作记录] 莱茵生命前端重设计全部完成并通过 QA（团队已归档） — rhine-lab-ui-redesign 全部 8 个任务完成并已归档团队。最终验收 verdict=pass：lint/type:check/test/build 全过（type:check 为基线 17 个既有错误、0 新增），audit-faction-ui 0 error（4-5 个信息性 warning），palette separation 12/12 唯一 tone lane，php artisan serve 冒烟主要页面 200 或匿名 302→login，data-terra-faction/depth 与 data-widget/toggle/mark 钩子在场，灰度/轮廓/布局碰撞测试通过。QA 期间发现并修复浏览器渲染才暴露的 AdminLTE/Bootstrap 级联回归，terra.css 追加第 11 段 Cascade hardening（html[data-terra-faction] 前缀 + 选择性 !important）。改动均为 CSS/Twig class 钩子/emotion 样式，未改功能、表单字段、路由与 JS 钩子。
- [2026-09-18 22:47] [工作记录] 浅色模式适应性修复：样本托盘 + 徽章矿物化 + 色块边界 — 修复浅色模式下浅色资源不可辨问题（仅 CSS/Twig class，无功能改动）。terra.css 新增第 12 节 Adaptive specimen tray：给透明预览 PNG 与 3D 查看器 canvas 加矿物棋盘托盘（浅色 --terra-tray-a #7d8b84 / b #8b9992，对浅色皮肤 ≥3.18:1、对深色皮肤 ≥3.98:1；暗色 #4d5c55/#586a62），terra-tray class 加到 SkinLibrary/Item、ClosetItem、Players/Viewer2d、ReportsManagement/ImageBox、components/Viewer（3D 舞台）；第 13 节把 AdminLTE/Bootstrap 预设徽章色（bg-teal/indigo/olive/navy/purple/maroon/green/yellow/red…）映射到矿物调色板（实测 bg-teal 白字仅 2.13:1）；.btn-color 非 terra-swatch 加 2px 矿物边（白/灰色块可见）；skinlib/base.twig 顶栏补 terra-top-band。验证：build/lint PASS、type:check 17 基线错误 0 新增、test 39 套件/366 用例 PASS、公共页对比审计 0 个 <3.2:1 元素、audit-palette-separation PASS。
- [2026-09-19 00:19] [工作记录] 前端重设计与插件修复已归档推送（commit dae3d72） — 把本机工作副本的全部未提交改动归档并推送：commit dae3d72（80 files, +8159/-90）→ https://github.com/HeDaas-Code/blessing-skin-server-custom 的 master（83a3588..dae3d72），git ls-remote 已确认远端为 dae3d726f472…。关键约束：工作目录 /home/hedass/文档/项目/blessing-skin-server 的 remote origin 仍是上游 https://github.com/bs-community/blessing-skin-server.git（分支 dev，reflog 只有一次 clone、从未提交），绝不能在该目录 push；正确做法是把改动同步到归档仓库 /home/hedass/文档/项目/blessing-skin-server-custom（origin 指向 HeDaas-Code 私有仓库）后在归档仓库提交。同步方式：rsync -rc（内容比较而非时间戳，避免 mtime 误判）+ --exclude 列表。本次提交内容：新增 resources/assets/src/styles/terra.css(57KB) 与 terra.ts、docs/terra-faction-ui/（39 个规约/令牌/审计脚本）、webpack.config.ts 双入口、20 个 React 组件/样式、13 个 twig（含 7 个 base.twig 的 data-terra-faction/depth）、plugins/yggdrasil-api 的 Profile::resolvePlayerFromUuid/isBoundUuid 与 SessionController 修复、PROJECT_MEMORY.md。刻意排除（避免回退归档仓库的定制）：.gitignore、README.md、plugins/.gitignore；以及不上传的 .env、database/*.sqlite、public/app 构建产物、resources/views/assets/*.twig、.agent-teams/、normify-blessing-skin-server/（两边同为 171 模块、无漂移所以未覆盖）。提交前校验：git ls-files 确认未跟踪 .env 与 sqlite、diff 内无 token/secret 明文、两个改动 PHP 文件 php -l 通过、插件全部 lang/*.yml YAML 解析通过。推送认证用 git -c credential.helper='!gh auth git-credential'（gh 已登录 HeDaas-Code）。
- [2026-09-19 01:41] [工作记录] Docker 一键部署包制作完成并推送归档仓库（commit aa07dc0） — 交付内容（工作副本 /home/hedass/文档/项目/blessing-skin-server，已同步到归档仓库并 push 到 HeDaas-Code/blessing-skin-server-custom 的 master=aa07dc0，14 files +913/-106）：1) 根 Dockerfile 多阶段：composer:2 装生产依赖（--no-dev --no-scripts --no-autoloader --ignore-platform-reqs）→ node:20-alpine 跑 yarn build（产出 public/app 与 resources/views/assets/*.twig）→ php:8.3-apache 运行时（install-php-extensions 装 gd/zip/imagick/pdo_mysql/opcache，自写 vhost + security.conf + php.ini，非 root 入口，VOLUME /app/storage，HEALTHCHECK curl /）。2) docker/entrypoint.sh（250 行）：首启生成 storage/.env（APP_KEY 随机、.env 通过软链 /app/.env 指向数据卷）、按环境变量刷新 .env（awk 定位+转义 \\ " $）、等 DB、migrate、key:generate、passport:keys、bs:install 建超管、写入 install.lock、逐个 plugin:enable 内置插件、同步 plugins/*/assets 到 public/plugins、view:clear；再启幂等。3) docker-compose.yml：app（build . / env_file deploy.env required:false / 卷 storage）+ 可选 mysql profile（--env-file deploy.env --profile mysql）。4) deploy.env.example（BS_ADMIN_EMAIL/PASSWORD/NICKNAME、BS_PORT、BS_SITE_NAME/URL、DB_*、MAIL_*、BS_PLUGINS）。5) docker/build.sh（buildx 构建 + docker save|gzip 导出 dist/<tag>-image.tar.gz）与 docker/deploy.sh（可选导入 tar、自动生成 deploy.env、按 DB_CONNECTION 自动加 --profile mysql、轮询就绪）。6) README-docker.md（快速开始/配置/MySQL/离线部署/备份/升级/Nginx 反代/运维/内部说明/排障）。7) Dockerfile.dockerignore 与 .dockerignore（内容一致，构建上下文从 ~108MB 压到源码量级）。验证（本机 docker 29.8.1 + buildx 0.37.1）：镜像 blessing-skin:6.0.2 = 778MB；容器首启自动安装成功（超管 admin@example.com permission=2 verified=1、4 个插件启用、uuid/ygg_log/ygg_bindings 建表、install.lock 写入）；首页 200、/admin 200、登录成功（密码含 #$&| 等特殊字符）；Yggdrasil 接口 profile 返回 204、authenticate 返回 403 JSON；重启幂等（“Nothing to migrate”、数据与 APP_KEY 保留、health=healthy）；docker compose --env-file deploy.env up -d 与 ./docker/deploy.sh 两条一键路径均端到端通过。离线交付物：dist/blessing-skin-6.0.2-image.tar.gz = 276MB（gzip -t 与 OCI index.json 结构校验通过）。
- [2026-09-19 11:56] [工作记录] 已在 10.63.127.239（TrimNAS/R730）用 Docker 部署 Blessing Skin 并验证通过 — 目标机：10.63.127.239（hostname R730，Debian 12 bookworm，TrimNAS，80 核 / 47GB 内存，docker 28.5.2 + compose v2.40.3，docker data-root=/vol2/docker）。部署目录 /vol1/1000/blessing-skin（NAS 用户 HeDass 的家目录卷，新建目录 mode 为 000，需先 chmod 700 才能写入），内含 blessing-skin-6.0.2-image.tar.gz(276MB)、docker-compose.yml、deploy.env。步骤：1) 该机 SFTP 通道不可用（scp 报 No such file or directory），改用 tar 经 ssh 管道上传，276MB 耗时约 3m52s（≈1.2MB/s）；2) 用用户提供的密码走 sudo -S 执行 docker load -i，得到镜像 blessing-skin:6.0.2(778MB)；3) cd /vol1/1000/blessing-skin 后 sudo docker compose --env-file deploy.env up -d；4) 首启自动安装成功：超管 uid=1（用户提供的 outlook 邮箱，permission=2 SUPER_ADMIN，verified=1）、4 个内置插件全部启用、uuid/ygg_log/ygg_bindings 建表、install.lock 写入；5) usermod -aG docker HeDass 后新会话可免 sudo 管理 docker；6) 在 deploy.env 追加 BS_SITE_URL=http://10.63.127.239:8080 并 up -d 重建容器，site_url 选项随之更新。对外地址 http://10.63.127.239:8080（宿主机 80/443 已被 NAS 自带服务占用，故用 8080）。验证：首页 200 且标题为 Blessing Skin、/auth/login 登录返回 code=0、/user 与 /admin 均 200、Yggdrasil profile 返回 204、authenticate 返回 403 JSON、/plugins/yggdrasil-api/assets/config.ts 与 /plugins/config-generator/assets/generator.js 均 200、容器 health=healthy、restart=unless-stopped、restarts=0、内存约 69MB。数据在卷 blessing-skin_storage（位于 /vol2/docker，挂载盘约 709G 可用）。本地临时 askpass 文件（含密码）已 shredded 删除。

## 经验教训 Lessons Learned

- [2026-09-17 23:48] [经验教训] 本机环境与 normify 使用要点 — sudo 实为 doas 别名，不支持 `sudo ENV=x cmd`，须写 `sudo env DEBIAN_FRONTEND=noninteractive apt-get install ...`。normify 默认 rootDir 为 /home/agentuser/workspace（其中 项目/projects 是指向 /home/hedass/文档/项目 的软链），可用 dir 参数把结构数据目录放进仓库内。normify_fingerprint 对空 source 返回 e3b0c442…b855（完整 SHA-256 hex）。仓库体量：PHP 后端约 7.8k 行、前端 TS/TSX 约 9.3k 行、Twig 模板约 100 个文件。
- [2026-09-17 23:53] [经验教训] normify_module_batch 写入要点 — upsert 模式的 items 形如 [{ frontmatter }]；根模块 parent 传 JSON null、非根由 id 去尾段推导。指纹必须逐模块调用 normify_fingerprint（repoRoot + 该模块 source 列表）获得，不能手算；一次 run_code 内可连续多次调用。source 路径缺失会让工具返回 missing 并导致校验失败。
- [2026-09-18 00:03] [经验教训] normify 批量写入的三个硬约束 — 1) normify_module_batch 是整批原子：任一模块 L1 失败则全批不落盘，故需小批或分组写入才能定位问题模块。2) dep/target-missing：deps.to 指向的模块必须已存在或在同一批内，因此父容器、被依赖叶子要先写。3) structure/label-too-long：dep label.en 必须 ≤30 字符（“Listen to registration completed” 32 字符即被拒）。4) normify_layout_upsert 对还没有子模块的模块报 layout/not-container，渲染数据必须在子模块写入之后写。
- [2026-09-18 02:07] [经验教训] BS User 模型创建需先填 password 占位 — 用 Eloquent 直接创建 User 时 users.password 是 NOT NULL 且不在 $fillable，但直接属性赋值可写。正确顺序：$user->password="temporary"; $user->save(); 再 $user->changePassword($raw)（内部走 cipher->hash + Filter）。若先 save 再 changePassword 会触发 Integrity constraint violation: users.password。
- [2026-09-18 09:17] [经验教训] Azure 重定向 URI 必须用 localhost 且三处一致 — 微软 OAuth 绑定（yggdrasil-api）本地联调约束：Azure 只给 http://localhost 开 HTTP 回环特例，http://127.0.0.1 会被拒。因此本地填 http://localhost:8000/user/yggdrasil-binding/callback，且三处必须完全一致：Azure 应用注册 Web 重定向 URI、插件配置 ygg_official_redirect_uri、浏览器访问用 localhost。当前站点 site_url 为 http://127.0.0.1:8000，插件自动生成的重定向会是 127.0.0.1，必须手动覆盖或改 site_url。Azure 应用还需选个人 Microsoft 账户并加 XboxLive.signin 权限。
- [2026-09-18 09:37] [经验教训] login_with_xbox 403 Invalid app registration = Azure Client ID 需 Mojang 白名单 — yggdrasil-api 官方绑定 403 最终根因（已由 Minecraft 官方文档确认）：Mojang 现已要求新创建的 Azure 应用先申请白名单才能调用 api.minecraftservices.com，未批准的应用在 login_with_xbox 返回 {"errorMessage":"Invalid app registration"}。与 XboxLive.signin 权限无关（不用再找它）。修复：填 https://aka.ms/mce-reviewappid 表单（邮箱、应用名、Client ID、Tenant ID、关联网站/域名、用途说明），等待 Mojang 审核约一周。官方说明文章 https://help.minecraft.net/hc/en-us/articles/16254801392141；参考实现文档 mcproto authentication.md、minecraft-launcher-lib microsoft_login.rst。审核期间站内 P0 离线认证不受影响。
- [2026-09-18 10:56] [经验教训] Prism Launcher 公开 Client ID 可临时测试 Mojang 服务链 — 已验证：PrismLauncher 仓库 CMakeLists 公开的 Azure Client ID `c36a9fb6-4f2a-41ff-90bd-ae7cc92031eb` 已通过 Mojang 白名单。yggdrasil-api 已实现设备码绑定模式（device code flow，scope `XboxLive.signin offline_access`，无需 secret/回调），后台可切换 device_code|authorization_code。限制：设备码绑定产生的 refresh_token 与 Prism client_id 绑定，切回授权码后旧绑定同步会失败；仅限临时测试，生产仍须给自己的 Azure 应用申请白名单 https://aka.ms/mce-reviewappid。
- [2026-09-18 13:23] [经验教训] oauth 插件 vendor 损坏导致启用后全站 500 — 本地环境 oauth 插件（plugins/oauth）vendor 不完整：vendor/symfony 目录为空（缺 polyfill-php72 等），且自带旧版 Illuminate 与 Laravel 10 的 Response 类签名冲突；一旦启用，PluginManager boot 加载其 vendor 即 500，并连累后台其它插件（如 config-generator）看起来启用失败。离线无法 composer 修复，已保持禁用。当前启用 4 个：yggdrasil-api、204-for-unexisted-players、hide-advanced-menu、config-generator，站点 200。经验：直接改 plugins_enabled 前先停掉 php artisan serve，否则运行中的服务进程可能把内存里的旧启用列表写回 DB。
- [2026-09-18 14:05] [经验教训] AgentTeams 两阶段审批协议要点 — AgentTeams 正确流程：先 agent_teams_status 查状态；无活动团队时用 agent_teams_create 且 approval="required" 一次性提交完整 members+tasks DAG 建 staged 计划，然后停下让用户审阅 Web 计划；绝不与 create 同轮调用 agent_teams_approve。用户在后续轮次明确批准（如“通过”）且计划已 staged 时，才可 approve 启动。此前手动写入 PROJECT_MEMORY.md 的“AgentTeams 工具未暴露”结论已过时（后续轮次工具已可用），本结论替代之。
- [2026-09-18 14:27] [经验教训] AgentTeams 成员会话可能无 team 工具，队长接管记录完成 — AgentTeams 子代理（成员）会话可能只暴露 bash/str_replace_editor/skill/exit_plan_mode，没有 agent_teams_claim/update/send_message 工具；此时成员照常完成工作并向队长报告，任务在 team.json 里保持 claimed。队长收报告后不能直接 update 成员拥有的任务，必须先用 agent_teams_reassign_task(assignee="captain") 接管（返回新 attempt_id），再用该 attempt_id 把任务 update 为 completed；随后下游任务自动进入 ready 并由调度器派发。不要直接改 .agent-teams/ 文件。
- [2026-09-18 14:53] [经验教训] setup/errors 页面走 spectre 入口，不加载 style 入口 — Blessing Skin 前端有两个独立样式入口：主站页面（home/user/admin/skinlib/auth）走 webpack 的 style 入口（common.css+AdminLTE+terra.css）；setup/errors 页面走 spectre 入口（spectre.css+fonts+spectre.css 自定义）。因此只给 base.twig 加 data-terra 属性不会让 setup/errors 加载 terra.css；主题化这两页时必须在 webpack.config.ts 的 spectre 数组里追加 '@/styles/terra.css'（或对应 styles 数组），否则令牌不生效。已转达 t4。
- [2026-09-18 15:05] [经验教训] AgentTeams 勿提前唤醒未就绪任务的成员 — 在 AgentTeams 中不要用 send_message 提前唤醒/指导某成员，除非其任务已经 ready：消息会把成员从 ready 状态拉起执行，若该成员的任务仍有未满足依赖（如 t4 依赖 t3 未完成），其子代理会空跑并以无内容失败结束，产生噪音。正确做法：把补充指导写进任务描述或等任务变为 ready 后由调度器派发，成员开始执行时再通过报告协调。本次 public-surfaces 因被提前唤醒而失败，无任务影响，其状态自动回到 ready。
- [2026-09-18 15:21] [经验教训] AgentTeams 成员限流失败与队长长轮转超时的处置 — AgentTeams 成员子代理会因上游限流/容量保护（PI_AI_ERROR）或 570s 长轮转超时而失败，任务被标记 failed、成员回到 ready。处置：不要立即重发相同请求（可能持续烧配额）；用 agent_teams_reassign_task 把任务重派给原成员或其他成员即可重试，重试前可稍等片刻。队长侧也可能因上下文过大出现 570s 超时；控制每轮上下文、缩小成员任务描述与报告体量，比反复重试更有效。
- [2026-09-18 16:29] [经验教训] 成员回合中断时用 send_message 让其继续已认领任务 — AgentTeams 成员子代理可能在没有产出任何文件改动时就提前结束回合（例如超时、上下文中断），其已认领任务仍保持 claimed、成员回到 idle/ready。此时正确做法是 send_message 唤醒该成员并明确“继续完成 task X + 当前应做事项”，而不是 reassign 重开新 attempt（无谓地丢失 attempt 计数），更不是替它完成。若其已有未完成尝试但无产出，reassign 也并非必需。
- [2026-09-19 00:01] [经验教训] 透明预览图/画布在浅色主题下必须给对比托盘；本地开发口令已重设为 password123 — 1) Blessing Skin 的皮肤/披风预览是透明 PNG，3D 查看器 canvas 也透明——放在近白卡片上时浅色皮肤几乎不可见，且这是 lint/test/build 查不出的渲染问题，必须用无头 Chrome + CDP 计算样式/截图亮度实测。解决：给预览容器加中明度矿物托盘（对浅色皮肤 #f2f2f2 与深色皮肤 #2b2b2b 都 ≥3:1）。2) AdminLTE 预设徽章色（bg-teal 白字 2.13:1 等）在浅色模式普遍不达标，需按矿物调色板统一映射。3) 本机 SQLite 开发库口令后续已重设：经 password_verify（BCRYPT）实测，admin@example.com 与 test@example.com 的当前有效口令均为 password123，故旧记录中的 Admin@123456 / Test@123456 已失效。登录态视觉核验仍应优先用下面这条已验证结论，不要复用旧口令；截图审计脚本见 /tmp/audit_light.py、/tmp/check_tray.py、/tmp/check_dark.py（临时文件，可能不持久）。
- [2026-09-19 00:19] [经验教训] 归档仓库推送约束：origin 指上游、rsync 需 -c、排除定制文件 — 1) 本仓库是上游 clone，remote origin 仍指向 bs-community/blessing-skin-server，任何 git push 前必须先 git remote -v 确认；本项目所有推送只去归档仓库 blessing-skin-server-custom（同内容但 origin 指向 HeDaas-Code）。2) 跨目录同步用 rsync -rc（按内容比较）：rsync 默认按 size+mtime 判断，会把日期更晚但内容相同的文件误判为需要传输（本次 docs/、plugins/ 整目录误报），-c 后差异从 134 项收敛到真实的 93 项。3) 归档仓库刻意为定制版：.gitignore 含 database/*.sqlite 与 storage 运行时目录，plugins/.gitignore 用 '!yggdrasil-api/' 放行插件（上游版只有 '!.gitignore'），README.md 也改写；同步时必须显式排除这三者，否则会把归档定制回退成上游版。4) 归档仓库里 resources/views/assets/*.twig 与 public/app 是构建产物而非源文件，无需（也不应）手动同步。5) 归档提交可用 git commit --author 指定 HeDaas-Code 作为作者：该机 git config --global 的 user.name/email 是 r4-qa（AgentTeams 遗留），不改全局配置即可让 GitHub 显示正确作者。
- [2026-09-19 01:41] [经验教训] Docker 化 Blessing Skin 的 9 个硬坑（Dockerfile/compose/Laravel/网络） — 1) COPY --from=<stage> 不支持 ARG 变量展开（报 'variable expansion is not supported for --from'）：要么写死 tag，要么 `FROM composer:${VER} AS composer` 起名后再 COPY --from=composer。2) `<dockerfile>.dockerignore` 优先级高于根 `.dockerignore` 且完全替代它（实测 Docker 29 + buildx 0.37：Dockerfile.dockerignore 生效时 .dockerignore 被忽略）；无前导斜杠的模式锚定上下文根（`storage/` 不匹配 plugins/x/storage/）。为兼容旧构建器可让两个文件内容一致。3) docker build 无 buildx 只有 legacy builder（会告警）；本机 apt 锁被外部 prusa-slicer 长期占用，改用 `gh release download --repo docker/buildx` 下载插件二进制装到 /usr/local/lib/docker/cli-plugins/docker-buildx 即可启用 BuildKit（无需 apt）。4) compose 的 env_file 值同样会被 `$` 插值：含 $ 的密码必须写 `$$`（`a$b` 会被吃成 `a`），或整值用单引号 `'x$y'`（双引号 `"p$q"` 仍会被插值）。5) compose 里 `environment:` 的优先级高于 `env_file:`，所以 DB_*/端口等不要写在 environment，否则 deploy.env 改了不生效；端口等插值变量一律用 `docker compose --env-file deploy.env` 才能读到。另有：compose `env_file: {path, required:false}` 允许文件缺失。6) Apache 的 ServerTokens/TraceEnable/ServerName 等是全局指令，写进 <VirtualHost> 会 AH00526 启动失败；应放 conf-available 全局文件（ServerName blessing-skin 还能消掉 AH00558）。7) Laravel dotenv 不接受未加引号的值里有空格（`APP_NAME=Blessing Skin` → 'Encountered unexpected whitespace'），生成 .env 必须给含空格/特殊字符的值加双引号并转义 \\ " $；用 awk 的 ENVIRON 写值可避免 sed 定界符/`&` 冲突。8) 生产镜像用 composer --no-dev 时 `php artisan tinker` 不存在（laravel/tinker 属 require-dev），脚本里别用 tinker；改为写一个引导 vendor/autoload.php + bootstrap/app.php + Console Kernel 的小 PHP 文件调 option()。9) 被 .dockerignore 排除的目录在构建阶段也不存在：webpack 的 HtmlWebpackEnhancementPlugin 写 resources/views/assets/*.twig 前必须 `mkdir -p resources/views/assets`，否则 'ENOENT: no such file or directory … app.twig'。附属经验：国内网络下 docker.io 元数据/依赖下载易 ECONNRESET/超时，composer 用 mirrors.aliyun.com/composer、yarn 用 registry.npmmirror.com 并保留官方源回退；github.com 的 install-php-extensions 可用 cdn.jsdelivr.net 作第一来源。Laravel 登录字段是 `identification`（不是 email），CSRF token 在 `<meta name="csrf-token" content="…">`，POST /auth/login 成功后返回 {"code":0,…redirectTo}。
- [2026-09-19 11:57] [经验教训] 远程部署到 NAS（TrimNAS）的可用姿势：SFTP 不可用改 tar 管道、目录默认 000、宿主机 80/443 已占用 — 1) 该 NAS 的 SSH 不提供可用 SFTP 子系统：scp（默认 SFTP 与 -O 旧协议都试过）会报 “No such file or directory / cp: 无法创建普通文件”，但 `ssh 'cat > 远端文件'` 正常。可靠做法是用 tar 经 ssh 管道：`tar cf - -C <本地目录> <文件...> | ssh 主机 'tar xf - -C <目标目录>'`，或 `cat 文件 | ssh 主机 'cat > 远路径'`。2) NAS 上 /vol1/<uid> 这类用户卷默认权限是 000（配合 ACL，`ls -l` 显示 d---------），mkdir 出来的子目录同样 000，写入报 ENOENT 而非 EACCES 容易误判；需先 `chmod 700 <目录>`。3) 该机 HeDass 不在 docker 组且 sudo 需密码：用 `echo <密码> | sudo -S -p '' docker ...` 可非交互执行；注意 sudo -S 会占用 stdin，不能让同一管道既喂密码又喂数据（此时先 `usermod -aG docker <用户>`，再用新 SSH 会话免 sudo 操作，组权限在新登录才生效）。4) 无 sshpass/paramiko 时，用 OpenSSH 自带的 SSH_ASKPASS 机制实现密码登录：写一个只 echo 密码的可执行脚本，`SSH_ASKPASS=/path SSH_ASKPASS_REQUIRE=force setsid -w ssh -o PreferredAuthentications=password -o PubkeyAuthentication=no -o StrictHostKeyChecking=accept-new 用户@主机 '命令'`；收尾务必 shred 删除该脚本。5) NAS 宿主机常自带 Web 服务占用 80/443，部署前先 `ss -tln | grep -E ':(80|443)\b'`，改用 8080 等端口。6) 该机用户家目录 /home/<user> 不存在，每次登录都会打印 “Could not chdir to home directory”，不影响命令执行但会污染输出，解析时要忽略。7) 容器重建（compose up -d --force-recreate）不会触发重装：install.lock 在数据卷内，入口脚本据此跳过 bs:install；本次只改了 deploy.env 的 BS_SITE_URL 就完成了 site_url 更新。

## 行动指南 Action Guide

- [2026-09-17 23:48] [行动指南] 下一步：normify 建树与渲染 — 架构图已覆盖 bss + yggdrasil-api 双树，官方认证容灾变更已 verified。部署注意事项：需在 Azure AD 注册 Web 应用并配置 ygg_official_client_id/secret/redirect_uri，官方路由默认关闭（ygg_official_enabled=false，此时绑定用户走 P2 本地兜底官方 UUID）。后续可选：绑定时同步官方皮肤、为绑定页加用户中心菜单入口、锚定 47 条未锚定箭头。
- [2026-09-18 09:35] [行动指南] 绑定失败排查：等用户重试后读 laravel 日志 — 微软绑定回调失败排查中：已开启 .env YGG_VERBOSE_LOG=true，BindingController 两处 catch 已加 Log::error 写 laravel 日志（含 trace）。已排除项：Binding 模型/Crypt/DB 读写正常、SkinSynchronizer 无皮肤路径正常、本机到 login.microsoftonline.com/user.auth.xboxlive.com/xsts/api.minecraftservices.com/textures.minecraft.net 网络可达（HTTP 200/404/403/400）。下一步：用户重试绑定后读 storage/logs/laravel-*.log 拿真实异常。注意：ygg 日志通道默认是 NullHandler（YGG_VERBOSE_LOG=false 时静默丢弃），排查必须显式开。
- [2026-09-18 14:05] [行动指南] 下一步：真机部署已完成，待办为 Azure secret 轮换与前端 warning 审计 — 已完成：1) 前端重设计推送归档仓库（dae3d72）；2) Docker 一键部署包制作并推送（commit aa07dc0，master）；3) 在 10.63.127.239 用离线镜像真实部署并验证（http://10.63.127.239:8080，超管为用户提供的 outlook 邮箱，4 插件启用，Yggdrasil 接口正常，容器 healthy；部署目录 /vol1/1000/blessing-skin）。仍未做：1) 到 Azure 门户轮换聊天中暴露过的 ygg_official_client_secret，再回填后台并做一次微软绑定联调（本地插件选项里 ygg_official_enabled=true）；2) 若对外提供访问，为该站点配 HTTPS/反代（NAS 的 80/443 已被占用，需另选端口）并把 deploy.env 的 BS_SITE_URL 改成域名；3) 审计 4 个信息性前端 warning（--terra-radius 命名、无 range 步进控件、缺 data-terra-element/layout/interaction/control 根属性）；4) 可选：把 config-generator、hide-advanced-menu、204-for-unexisted-players 三个第三方插件纳入归档仓库（当前仅 yggdrasil-api 受版本控制）。运维要点：目标机 HeDass 已加入 docker 组，管理命令为 `cd /vol1/1000/blessing-skin && docker compose --env-file deploy.env <up -d|logs -f app|down>`；备份用 `docker run --rm -v blessing-skin_storage:/data -v $PWD:/backup alpine tar czf /backup/storage-$(date +%F).tar.gz -C /data .`。注意：工作副本 /home/hedass/文档/项目/blessing-skin-server 的 origin 仍是上游，切勿在该目录 push；同步归档仓库仍用 rsync -rc + 排除清单，再 `git -c credential.helper='!gh auth git-credential' push origin master`。

## 备注 Notes

- [2026-09-18 12:23] [备注] 本地开发账号与密码 — 本地 SQLite 环境账号：admin@example.com / Admin@123456（超管，2026-09-18 因原密码未记录已重置）；test@example.com / Test@123456（管理员，原密码，Hash::check 验证有效）。仅限本地开发环境，非生产凭据。
- [2026-09-19 00:01] [备注] 本地开发库超级管理员账号已确认（admin@example.com / password123） — 从本机 SQLite 库（database/database.sqlite，DB_CONNECTION=sqlite）直接读取 users 表并用 PWD_METHOD=BCRYPT 的 password_verify 校验，确认超级管理员账号：uid=1 / email admin@example.com / nickname admin / permission=2（User::SUPER_ADMIN）/ verified=1，当前口令 password123；另一账号 uid=2 test@example.com 为 permission=1（普通管理员），口令同为 password123。适用范围：仅本机开发库，非生产凭据；users.permission 中 2=SUPER_ADMIN、1=管理员由 app/Models/User.php 定义，权限判定见 app/Http/Middleware/CheckRole.php。重置口令的正确姿势是 php artisan tinker 里 User::find(1)->changePassword('<新口令>')，不要直接改 password 字段。PROJECT_MEMORY.md 与旧记忆里的 Admin@123456 已失效，以本条为准。
